<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE package SYSTEM "http://pear.php.net/dtd/package-1.0">
<!-- do not use the "Type" attribute here, that one is only for
     generated package.xml files -->
<package version="1.0">
  <name>HTTP_Upload</name>
  <summary>Easy and secure managment of files submitted via HTML Forms</summary>
  <description>
This class provides an advanced file uploader system for file uploads made
from html forms. Features:
 * Can handle from one file to multiple files.
 * Safe file copying from tmp dir.
 * Easy detecting mechanism of valid upload, missing upload or error.
 * Gives extensive information about the uploaded file.
 * Rename uploaded files in different ways: as it is, safe or unique
 * Validate allowed file extensions
 * Multiple languages error messages support (es, en, de, fr, it, nl, pt_BR)
  </description>
  <maintainers>
    <maintainer>
      <user>cox</user>
      <name>Tomas V.V.Cox</name>
      <email>cox@idecnet.com</email>
      <role>lead</role>
    </maintainer>
    <maintainer>
      <user>wenz</user>
      <name>Christian Wenz</name>
      <email>wenz@php.net</email>
      <role>lead</role>
    </maintainer>
    <maintainer>
      <user>antonio</user>
      <name>Antônio Carlos Venâncio Júnior</name>
      <email>antonio@php.net</email>
      <role>lead</role>
    </maintainer>
  </maintainers>

  <release>
    <license>LGPL</license>
    <version>1.0.0</version>
    <date>2004-07-30</date>
    <state>stable</state>
    <notes>
- (antonio) Added support for both PHP >= 4.2 and PHP 4.1
- (antonio) Fixed a lot of bugs
- (antonio, wenz) Fixed some localization strings
- (wenz) if only empty files are uploaded, an error message 
  is triggered 
- (cox, antonio, wenz) many minor fixes
- (cox) Error detection was enhanced, so you no longer need to
  check for PEAR::isError() in $upload->getFiles() or call
  $upload->isMissing(). Instead you'll
  get the error when do a check for $file->isError().

  Example:

   $upload = new HTTP_Upload('en');
   $file = $upload->getFiles('i_dont_exist_in_form_definition');
   if ($file->isError()) {
      die($file->getMessage());
   }

TODO:

- Move error class to a new file
- As PEAR in general requires now PHP 4.2, maybe drop BC compat in future
    </notes>
    <filelist>
      <dir name="/" baseinstalldir="HTTP">
	<file role="php" md5sum="0872867b9f0a6545464476953f737b45">Upload.php</file>
	<dir name="docs" role="doc">
	  <file md5sum="2cb222ea90d1dbb425f170e11b9b6ca8">upload_example.php</file>
	</dir>
      </dir>
    </filelist>
  </release>
</package>
